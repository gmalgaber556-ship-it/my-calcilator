import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calculator.app',
  appName: 'آلة حاسبة متقدمة',
  webDir: 'dist/public'
};

export default config;
